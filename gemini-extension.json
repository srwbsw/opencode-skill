{
  "name": "second-agent-skill",
  "version": "2.17.0",
  "description": "Cross-engine second agent — delegate code reviews, second opinions, or engineering tasks (write tests, fix bugs, refactor) to another AI engine. Adds a /second-agent command that runs review.js (read-only) or agent.js (task delegation) against another engine (Gemini, Codex, Claude, opencode, …)."
}
